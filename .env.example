# IblalORM Environment Configuration
# Copy this file to .env and fill in your values

# Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=iblal_orm_db
DB_USER=postgres
DB_PASSWORD=postgres

# Schema to introspect (default: public)
DB_SCHEMA=public
